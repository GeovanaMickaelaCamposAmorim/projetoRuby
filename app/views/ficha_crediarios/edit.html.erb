<% content_for :title, "Editing ficha crediario" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing ficha crediario</h1>

  <%= render "form", ficha_crediario: @ficha_crediario %>

  <%= link_to "Show this ficha crediario", @ficha_crediario, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  <%= link_to "Back to ficha crediarios", ficha_crediarios_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
