<div class="container mx-auto px-4 py-8">
  <div class="max-w-4xl mx-auto">
    <div class="mb-6">
      <h1 class="text-3xl font-bold text-gray-900">Editar Cliente</h1>
      <p class="text-gray-600 mt-2">Editando: <strong><%= @cliente.cli_nome %></strong></p>
    </div>

    <div class="bg-white rounded-lg shadow p-6">
      <%= render 'form', cliente: @cliente %>
    </div>

    <div class="mt-4">
      <%= link_to '← Voltar para lista de clientes', clientes_path, class: 'text-blue-600 hover:text-blue-800 font-medium' %>
    </div>
  </div>
</div>